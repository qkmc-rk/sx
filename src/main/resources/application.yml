server:
  port: 8080

user:
  password: 123456
spring:
  # 数据源
  datasource:
    username: root
    password: 1314
    url: jdbc:mysql://10.255.251.34:3306/sx?useSSL=false&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    open-in-view: false
    show-sql: false
    hibernate:
      ddl-auto: update

  # 热重启
  devtools:
    restart:
      enabled: true

  # 允许同名bean覆盖注册
  main:
    allow-bean-definition-overriding: true

jodconverter:
  enabled: true
  # open office 安装路径
  office-home: C:\Program Files (x86)\OpenOffice 4
  max-tasks-per-process: 10
  port-numbers: 8100
  kill-existing-process: true

